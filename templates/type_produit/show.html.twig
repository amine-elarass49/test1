{% extends 'base.html.twig' %}

{% block title %}TypeProduit{% endblock %}

{% block body %}
    <h1>TypeProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Type_id</th>
                <td>{{ type_produit.typeId }}</td>
            </tr>
            <tr>
                <th>Type_libelle</th>
                <td>{{ type_produit.typeLibelle }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ type_produit.createdAt ? type_produit.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ type_produit.updatedAt ? type_produit.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_produit_index') }}">back to list</a>

    <a href="{{ path('type_produit_edit', {'type_id': type_produit.type_id}) }}">edit</a>

    {{ include('type_produit/_delete_form.html.twig') }}
{% endblock %}

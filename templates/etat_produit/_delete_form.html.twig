<form method="post" action="{{ path('etat_produit_delete', {'etat_id': etat_produit.etat_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat_produit.etat_id) }}">
    <button class="btn">Delete</button>
</form>

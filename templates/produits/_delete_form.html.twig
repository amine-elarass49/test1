<form method="post" action="{{ path('produits_delete', {'pr_id': produit.pr_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.pr_id) }}">
    <button class="btn">Delete</button>
</form>

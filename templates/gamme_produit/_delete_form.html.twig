<form method="post" action="{{ path('gamme_produit_delete', {'gam_id': gamme_produit.gam_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gamme_produit.gam_id) }}">
    <button class="btn">Delete</button>
</form>

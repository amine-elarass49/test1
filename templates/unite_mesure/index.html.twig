{% extends 'base.html.twig' %}

{% block title %}UniteMesure index{% endblock %}

{% block body %}
    <h1>UniteMesure index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Um_id</th>
                <th>Um_libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unite_mesure in unite_mesures %}
            <tr>
                <td>{{ unite_mesure.umId }}</td>
                <td>{{ unite_mesure.umLibelle }}</td>
                <td>
                    <a href="{{ path('unite_mesure_show', {'um_id': unite_mesure.um_id}) }}">show</a>
                    <a href="{{ path('unite_mesure_edit', {'um_id': unite_mesure.um_id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('unite_mesure_new') }}">Create new</a>
{% endblock %}

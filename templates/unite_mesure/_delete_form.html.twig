<form method="post" action="{{ path('unite_mesure_delete', {'um_id': unite_mesure.um_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unite_mesure.um_id) }}">
    <button class="btn">Delete</button>
</form>
